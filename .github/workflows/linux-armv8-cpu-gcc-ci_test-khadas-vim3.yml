name: linux-armv8-cpu-gcc-runner-khadas-vim3

on:
  push:
    branches: [tengine-lite, ci_op_1]
    paths-ignore: 
    - 'doc/**'
    - 'README.md'
    - 'README_EN.md'
  pull_request:
    branches: [tengine-lite, ci_op_1]
    paths-ignore: 
    - 'doc/**'
    - 'README.md'
    - 'README_EN.md'

jobs:
  linux-armv8-cpu-gcc-runner-test:
    runs-on: self-hosted, khadas-vim3
    steps:
    - uses: actions/checkout@v2.3.4

    - name: configure
      run: mkdir build && cd build && cmake ..
    - name: build
      run: cmake --build build -j 4 
